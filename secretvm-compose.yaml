services:
  website:
    image: popeblack/cv-website:latest
    container_name: cv-website
    restart: unless-stopped
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
